import { createGlobalStyles } from 'styled-componentes'

export default createGlobalStyles`
     * {
           margin: 0;
           padding: 0;
           box-sizing: border-box;
           font-family: 'Roboto', sans-serif;
           outline: none;
      }
`
